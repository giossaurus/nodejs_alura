// boolean
// true -> verdadeiro
// false -> falso

const primeiroNumero = 5;
const segundoNumero = 5;
let cadastroAtivado = false;

console.log(primeiroNumero === segundoNumero);

//Um igual apenas no JS é atribuir um valor a variável, dois ou três iguais são comparações, retorna true ou false.

const texto1 = "Alura";
const texto2 = "A";

console.log(texto1 === texto2);

//Usar comando git status para ver os arquivos, depois seguindo com git add, git commit -m "mensagem", git push origin master